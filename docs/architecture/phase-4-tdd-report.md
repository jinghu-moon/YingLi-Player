# Phase 4 TDD 报告

## 交付范围

- 完成纯 Kotlin 播放请求、状态、命令、状态仓库和进度仓库契约。
- 完成幂等状态归约器、3 秒续播规则、5 秒进度节流和六类错误映射。
- 完成唯一 `MediaSessionService`/`ExoPlayer` 作用域、应用级 Controller 连接和系统通知会话。
- 完成真实媒体卡入口、全屏黑色播放画布、视频 Surface、Loading、播放/暂停、拖动、结束与错误恢复 UI。
- 完成共享会话状态的迷你播放器栏；播放页继续隐藏一级导航。
- Room Schema 保持 v1，仅增加 DAO 查询和更新，不迁移表结构。

## TDD 证据

| 层级 | 覆盖重点 | 结果 |
| --- | --- | --- |
| JVM | 状态转移、重复/非法命令、续播边界、节流、错误映射、Fake Controller ViewModel | `testDebugUnitTest`：76 项通过 |
| Room 仪器 | 媒体位置解析、3 秒回退、进度更新 | 真机通过 |
| Media3 仪器 | Application 单例 Controller、真实 Service 连接、Idle 非法命令拒绝 | 真机通过 |
| Compose 仪器 | 黑色画布、Loading、播放/暂停语义、结束、权限错误恢复 | 真机通过 |

## 真机执行记录

- 设备：Xiaomi M2012K11AC，Android 13/API 33。
- 最终仪器回归：`OK (31 tests)`，耗时 28.065 秒，0 失败、0 跳过。
- 覆盖范围包括 Media3 Service/Controller、Room 播放进度、Compose 播放状态矩阵，以及既有媒体索引和自适应导航回归。
- MP4/HEVC/损坏/失权样本和点击到首帧基线仍需独立媒体样本测试，不计入上述自动化通过结论。

## 首帧基线模板

| 样本 | 编码/容器 | 点击到首帧 | 结果 |
| --- | --- | --- | --- |
| 标准 MP4 | H.264/MP4 | 待测 | 待复验 |
| HEVC | H.265/MP4 | 待测 | 待复验 |
| 损坏文件 | 不适用 | 不适用 | 待验证错误分类 |
| 撤销权限 URI | 不适用 | 不适用 | 待验证重新授权动作 |

目标仍为点击后 1 秒内显示首帧；超过时持续显示明确 Loading，不显示虚假进度。
