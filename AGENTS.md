# 开发阶段工程规则

当前项目处于**开发阶段，尚未正式发布**。

当前阶段的首要目标是：**解决根因、保持正确性、追求性能与合理架构，而不是维护历史实现。**

## 1. 兼容性

当前阶段**不考虑向后兼容性**，向后兼容不是当前阶段的设计约束。

允许并鼓励：

- 破坏性 API 修改；
- 删除旧实现；
- 重构数据结构；
- 调整模块边界；
- 重构调用链；
- 修改既有接口；
- 删除废弃代码；
- 重新设计不合理的抽象。

除非当前需求明确要求，否则不要为了兼容旧实现增加：

- Adapter；
- Compatibility Layer；
- Legacy API；
- Migration Logic；
- 临时兼容分支；
- 重复实现。

**可以破坏旧实现，但不能无意破坏当前已有功能。**

---

## 2. 根因优先

发现问题后，必须优先寻找**根本原因**，而不是只修复表面现象。

优先判断问题属于：

1. 局部实现错误；
2. 接口设计错误；
3. 数据结构错误；
4. 模块边界错误；
5. 调用流程错误；
6. 架构或抽象设计错误。

如果问题来自接口、数据结构、模块边界或架构，应优先修改对应设计，而不是在外层增加补丁。

禁止为了让当前代码“看起来能工作”而：

- 增加特殊判断绕过问题；
- 增加临时分支；
- 用包装层隐藏错误设计；
- 保留明显错误的抽象；
- 在错误的数据结构上继续堆功能。

---

## 3. 最小改动不是目标

**最小 Diff、最少改动和最少文件修改，不是目标。**

不要因为 Diff 更小、改动文件更少或短期风险更低，就默认选择局部修补方案。

如果：

- 方案 A 改动很小，但只是绕过问题；
- 方案 B 需要重构，但能够彻底解决根因；

并且方案 B 可以被充分验证时，**应优先选择方案 B**。

目标不是“少改代码”，而是：

> **用合理的设计，从根因上解决问题。**

---

## 4. 重构规则

如果现有设计明显不合理，允许直接：

- 删除；
- 重命名；
- 重构；
- 合并；
- 拆分；
- 重新设计；
- 替换整个实现。

不要因为：

- 旧代码已经存在；
- 改动范围较大；
- Diff 较大；
- 旧代码目前还能运行；

就默认保留错误实现。

不要为了减少改动范围而牺牲：

- 正确性；
- 性能；
- 架构质量；
- 简洁性；
- 可维护性。

### 决策优先级

多个方案都可行时，按以下优先级选择：

> **正确性 → 根因解决 → 性能 → 架构质量 → 简洁性 → 可维护性**

当前阶段**不以向后兼容性作为方案选择依据**。

### 性能要求

性能优化必须有明确依据，例如：

- 基准测试；
- Profiling / 性能剖析结果；
- 明确的性能热点；
- 明显的时间或空间复杂度问题；
- 已知的性能瓶颈；
- 可量化的资源占用问题。

不得为了臆测性能而增加不必要的复杂抽象、缓存、并发机制或数据结构。

---

## 5. 修改前分析

对于可能影响多个模块、公共接口、数据结构或核心流程的修改，实施前必须先确认：

- 当前实现是什么；
- 问题表现是什么；
- 问题根因是什么；
- 影响范围是什么；
- 是否属于设计问题；
- 是否需要重构；
- 修改后的目标设计是什么；
- 如何验证修改正确。

需要时，应先阅读相关代码、测试、配置以及调用方，理解完整调用关系后再修改。

**禁止在未理解关键代码路径的情况下直接修改核心实现。**

---

## 6. 测试与回归

**不考虑兼容性，不等于不需要回归测试。**

对于可能影响已有行为的修改，应按照：

> **修改前建立基线 → 实施修改 → 验证新行为 → 验证已有行为 → 对比结果**

进行验证。

至少确认：

- 新功能正常；
- 修改后的功能符合需求；
- 原有相关功能正常；
- 边界情况正常；
- 异常情况符合预期；
- 性能没有出现不可接受的退化；
- 无关功能没有被意外破坏。

已有测试必须优先复用并运行。

没有现成测试时，应通过可执行方式建立验证，例如：

- 单元测试；
- 集成测试；
- 构建检查；
- 静态检查；
- CLI 验证；
- 示例输入输出；
- 必要的人工验证。

### 验证必须真实有效

不得通过以下方式制造“验证通过”：

- 跳过测试；
- 注释测试；
- 弱化断言；
- 删除失败用例；
- 修改错误的测试预期以掩盖问题；
- 降低验收标准；
- 只运行无关测试；
- 只验证新增功能而不验证相关已有功能；
- 在没有实际执行验证的情况下声称验证通过。

如果测试因为当前设计发生合理变化，可以修改测试；但必须确认：

> **修改测试是因为预期行为发生了正确变化，而不是为了让错误实现通过测试。**

### 验证证据

完成修改时，应明确说明：

- 执行了哪些测试或验证；
- 使用了什么命令或步骤；
- 关键验证结果是什么；
- 修改前后的关键行为差异；
- 是否存在尚未覆盖的风险。

---

## 7. 禁止伪完成

以下情况不得认为问题已经解决：

- 只绕过错误；
- 只让单个用例通过；
- 只修改调用方而未处理根因；
- 未确认根因；
- 未进行必要测试；
- 留下临时兼容代码；
- 留下无效旧实现；
- 留下已知 TODO 却声称任务全部完成。

**“代码修改完成”不等于“任务完成”。**

任务完成必须满足：

> **根因解决 + 新功能正确 + 相关已有功能正常 + 验证通过**

---

## 8. 验收标准

一次修改只有同时满足以下条件，才视为完成：

1. 根因已经解决，而不是绕过问题；
2. 新需求已经正确实现；
3. 相关已有功能未被意外破坏；
4. 相关测试或验证已经通过；
5. 没有为了兼容旧实现而引入无意义复杂度；
6. 没有留下仅用于过渡的兼容代码；
7. 没有保留已知无效代码、重复实现或废弃接口；
8. 修改后的实现相比修改前，在正确性、性能、架构质量、简洁性或可维护性方面具有明确合理性。

**完成标准不是“代码改完”，而是“验证通过”。**

---

## 9. 关键行为对比

对于关键行为发生变化的修改，应记录修改前后的实际行为：

| 项目     | 修改前     | 修改后     | 预期结果                     |
| -------- | ---------- | ---------- | ---------------------------- |
| 原有功能 | 实际行为   | 实际行为   | 应保持正常，或按需求明确变化 |
| 修改功能 | 实际行为   | 实际行为   | 应符合新需求                 |
| 新增功能 | 不存在     | 实际行为   | 应符合需求                   |
| 边界情况 | 实际行为   | 实际行为   | 应正确处理或明确拒绝         |
| 性能表现 | 修改前指标 | 修改后指标 | 不应出现不可接受退化         |

---

## 核心原则

> **开发阶段不被历史实现束缚。**
>
> **不考虑向后兼容，但必须保证当前功能正确。**
>
> **可以破坏旧实现，但不能无意破坏现有功能。**
>
> **根因优先，重构优先于妥协。**
>
> **最小 Diff 不是目标，根因解决才是目标。**
>
> **性能优化必须有依据，禁止为了臆测性能而过度设计。**
>
> **代码改完不是完成，验证通过才是完成。**